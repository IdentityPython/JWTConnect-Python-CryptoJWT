language: python

sudo: false

python:
  - "3.6"
  - "3.7"

install:
  - pip install tox

env:
   - TOXENV=quality

matrix:
  include:
    - python: 3.6
      env: TOXENV=py36

script:
  - tox -e $TOXENV

notifications:
  email: false
