[tox]
envlist = py{35,36,37},quality

[testenv]
passenv = CI TRAVIS TRAVIS_*
commands =
    py.test --cov=cryptojwt tests/ {posargs}
    codecov
extras = testing
deps =
  codecov
  pytest-cov

[pep8]
max-line-length=100

[pytest]
addopts = --color=yes
